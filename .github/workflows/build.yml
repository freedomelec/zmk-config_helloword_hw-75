name: Build ZMK Firmware

on:
  push:
  pull_request:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    container:
      image: zmkfirmware/zmk-build-arm:stable

    steps:
      - name: Prepare variables
        run: |
          echo "zephyr_version=${ZEPHYR_VERSION}" >> "$GITHUB_ENV"

      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up build tools
        run: |
          set -eux

          apt-get update
          apt-get install -y \
            python3-pip \
            python3-venv \
            python3-setuptools \
            python3-protobuf \
            python3-pyelftools \
            protobuf-compiler \
            curl \
            git \
            ca-certificates

          curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
          apt-get install -y nodejs

          python3 -m venv /tmp/pytools
          /tmp/pytools/bin/pip install -U pip wheel
          /tmp/pytools/bin/pip install "setuptools<81"
          /tmp/pytools/bin/pip install -U west fonttools pyelftools
          /tmp/pytools/bin/pip install --force-reinstall "protobuf<6"

          ln -sf /tmp/pytools/bin/west /usr/local/bin/west || true
          ln -sf /tmp/pytools/bin/pyftsubset /usr/local/bin/pyftsubset || true
          ln -sf /tmp/pytools/bin/fonttools /usr/local/bin/fonttools || true

          npm install -g lv_font_conv

      - name: Cache west modules
        uses: actions/cache@v5
        continue-on-error: true
        env:
          cache_name: cache-zephyr-${{ env.zephyr_version }}-modules
        with:
          path: |
            modules/
            zephyr/
            zmk/
          key: ${{ runner.os }}-build-${{ env.cache_name }}-${{ hashFiles('**/west.yml', '**/build.yaml') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache_name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-

      - name: Prepare west
        run: |
          set -eux
          export PATH="/tmp/pytools/bin:$PATH"

          west init -l config
          west update --narrow || west update --narrow
          west zephyr-export
          git config --global --add safe.directory "$PWD"

      - name: Verify Python build deps
        run: |
          set -eux
          export PATH="/tmp/pytools/bin:$PATH"

          python -c "import sys; print(sys.executable)"
          python -m pip show setuptools
          python -m pip show protobuf
          python -c "import elftools; print('pyelftools OK')"
          python -c "import google.protobuf; print('protobuf OK')"
          python -c "import pkg_resources; print('pkg_resources OK')"

      - name: West build
        env:
          PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION: python
        run: |
          set -eux
          export PATH="/tmp/pytools/bin:$PATH"

          west build -s zmk/app -b "hw75_keyboard@1.2" -- \
            -DZMK_CONFIG="${GITHUB_WORKSPACE}/config" \
            -DKEYMAP_FILE="${GITHUB_WORKSPACE}/config/hw75_keyboard.keymap"

      - name: Collect artifacts
        run: |
          set -eux
          mkdir -p build/artifacts

          if [ -f build/zephyr/zmk.uf2 ]; then
            cp build/zephyr/zmk.uf2 build/artifacts/hw75_keyboard@1.2-zmk.uf2
          fi

          if [ -f build/zephyr/zmk.hex ]; then
            cp build/zephyr/zmk.hex build/artifacts/hw75_keyboard@1.2-zmk.hex
          fi

          if [ -f build/zephyr/zmk.bin ]; then
            cp build/zephyr/zmk.bin build/artifacts/hw75_keyboard@1.2-zmk.bin
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: firmware-hw75_keyboard-1_2
          path: build/artifacts
          if-no-files-found: error
